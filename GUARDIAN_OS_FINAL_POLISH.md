# Guardian OS Final Frontend Polish

This final small patch:

- Renames the web product interface to **Guardian OS**.
- Keeps **DriverGuardianAI** as the underlying project and repository name.
- Moves the sidebar collapse control away from the product title.
- Reduces brand typography slightly so the expanded sidebar feels balanced.
- Keeps the compact collapsed-sidebar presentation unchanged.

## Install

Stop FastAPI, copy the included `app` folder over the existing `app` folder,
restart on port 8010, then press `Ctrl+F5`.
